from __future__ import annotations

def generate_sql(question: str, kpi_evidence: str, model: str = "unused") -> str:
    """
    Deterministic no-API-key mode.
    Forces fallback to the grounded SQL Example in KPI evidence.
    """
    return "CANNOT_ANSWER"
